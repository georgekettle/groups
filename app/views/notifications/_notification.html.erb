<%= turbo_frame_tag dom_id(notification) do %>
  <% case notification.type
      when "MentionNotification" %>
      <%= render 'notifications/mention_notification', notification: notification, message: notification.params[:message] %>
  <% end %>
<% end %>
