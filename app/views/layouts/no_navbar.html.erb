<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="bg-tofu">
    <div class="body">
      <%= yield %>
    </div>
    <%= render 'shared/load_screen' %>  <!-- will disappear once tailwind is loaded -->
  </body>
</html>
