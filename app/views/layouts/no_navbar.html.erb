<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body>
    <div class="body">
      <%= yield %>
    </div>
    <%= render 'shared/navbar' %>
    <%= render 'shared/load_screen' %>  <!-- will disappear once tailwind is loaded -->
  </body>
</html>
