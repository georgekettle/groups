<% mobile_no_padding ||= false %>

<% if mobile_no_padding %>
  <div class="max-w-3xl mx-auto px-2 sm:px-0 lg:px-8">
    <%= yield %>
  </div>
<% else %>
  <div class="max-w-3xl mx-auto px-2 sm:px-6 lg:px-8">
    <%= yield %>
  </div>
<% end %>
