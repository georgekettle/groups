<%= form_for [@channel, @message],
    data: { controller: 'reset-form', action: 'turbo:submit-end->reset-form#reset' } do |form| %>
  <div class="form-inputs">
    <%= form.label :content %>
    <%= form.rich_text_area :content %>
  </div>
  <%= form.submit data: { reset_form_target: 'button' } %>
<% end %>
